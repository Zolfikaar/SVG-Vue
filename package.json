{
  "name": "svg-to-vue",
  "displayName": "SVG to Vue",
  "description": "Generate Vue components instantly from SVG icons. [Try the Web Version](https://svg-to-vue.app)",
  "version": "0.0.1",
  "publisher": "svg-to-vue",
  "engines": {
    "vscode": "^1.85.0"
  },
  "icon": "images/icon.png",
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:svg-to-vue.generateComponents"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "svg-to-vue.generateComponents",
        "title": "SVG to Vue: Generate Components",
        "category": "SVG to Vue"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "svg-to-vue.generateComponents",
          "when": "true"
        }
      ],
      "explorer/context": [
        {
          "command": "svg-to-vue.generateComponents",
          "group": "navigation@9",
          "when": "resourceDirname"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/svg-path-bbox": "^1.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.0.0",
    "vsce": "^2.15.0"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.11",
    "svg-path-bbox": "^2.1.0",
    "svgo": "^4.0.1",
    "svgpath": "^2.6.0"
  }
}
